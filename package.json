{
  "name": "codex-github-pilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.0.0" },
  "scripts": { "test": "node --test", "start": "node src/cli.mjs run", "doctor": "node src/cli.mjs doctor" }
}
